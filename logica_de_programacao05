string CalcularValorComDescontoFormatado(double valor, double desc)
            {
                string resultado;
                desc = desc / 100;
                valor -= valor*desc ;
                resultado = valor.ToString();
                return resultado;
            }
